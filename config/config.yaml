# Application configuration

schedule:
  ats: "0 */6 * * *"      # Companies from config/companies.yaml (Greenhouse, Lever, Ashby)
  rss: "0 */6 * * *"      # General feed scrapers (RemoteOK, WWR, Jobicy, freehire.dev)
  cleanup: "0 0 * * 0"    # Weekly on Sunday

notifications:
  telegram:
    enabled: true
    # Set these via environment variables:
    # TELEGRAM_BOT_TOKEN
    # TELEGRAM_CHAT_ID

storage:
  dbPath: "./data/jobs.db"
  retentionDays: 90

# Experience filter: exclude senior roles and jobs requiring too many years
experience:
  enabled: true
  maxYears: 3

# Location filter: only jobs in your country, remote, or with visa sponsorship
location:
  enabled: true
  userCountry: "MX"
  allowRemote: false
  allowVisaSponsorship: false
